public interface PaymentMethod {
    // Receipt processPayment(double amount, String fullAddress);
    // Define the method signature for processing payments
    // Note: You'll need to define the Receipt class and its structure
}
